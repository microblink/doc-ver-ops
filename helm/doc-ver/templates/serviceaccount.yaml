{{- if .Values.docVer.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "docver.serviceAccountName" . }}
  labels:
    {{- include "docver.labels" . | nindent 4 }}
  {{- with .Values.docVer.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
