{{- if .Values.singleImage.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "docver.fullname" . }}
  labels:
    {{- include "docver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.singleImage.service.type }}
  ports:
    - name: http
      port: {{ .Values.singleImage.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ include "docver.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
